import React from 'react';
import { Text, View } from 'react-native';

const BarcodeRegist = ({ barcode }) => {
  return (
    <View>
      <Text>BacodeRegister: {barcode}</Text>
    </View>
  );
};

export default BarcodeRegist;
